apiVersion: v2
name: rbac
description: A Helm chart for the RBAC platform (Django backend + React frontend + PostgreSQL)

# Application chart packaged into a versioned archive for deployment.
type: application

# Chart version — increment on chart/template changes (SemVer).
version: 0.1.0

# Version of the application being deployed.
appVersion: "1.0.0"
